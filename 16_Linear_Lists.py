# Concatenation
numbers = [1, 2, 3]
letters = ['a', 'b', 'c']
numbers + letters
letters + numbers
numbers
letters
dir(numbers)
help(numbers.reverse)
#reverse function
numbers.reverse()
numbers
#pop function = removes the last one
numbers.pop
numbers
# insert function = add item in order
numbers.insert(2, 1)
numbers
# showing item position
numbers.index(1)
